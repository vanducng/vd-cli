[tools]
# Matches go.mod, desktop/go.mod, and the "1.23" pins in .github/workflows/{test,release}.yml.
# modernc.org/sqlite is pinned to v1.39.0 for the same reason: v1.39.1+ require go >= 1.24.
go = "1.23.4"
node = "22"
